# PQ-AI 验证报告（2026-07-19）

## 验证范围

- 生产数据与人工训练宽表同等参与数据集构建。
- 点位参数调整派生完整喷涂程序版本。
- 云端、虚拟线、上位机三方参数快照和受控远程发布。
- 独立新增 7 表审批 SQL 与总 DDL 一致性。
- CodeRabbit 新增后端、前端和上位机程序候选问题修复。

## 自动化结果

- 后端：`274 passed, 1 skipped`。
- 前端：ESLint、`tsc --noEmit`、Next.js 生产构建均通过；生产构建包含 34 个页面。
- DDL：ORM 与总 DDL 均为 99 张表；独立 SQL 恰好包含指定 7 张新表，列与 ORM 一致，无物理外键、建库语句及运行时禁用操作。
- 依赖：`npm audit --omit=dev --audit-level=moderate` 为 0 漏洞；`pip-audit --strict .` 无已知漏洞。
- 安全：仓库内本地账号、临时密码、私钥头及非占位敏感变量扫描无命中。
- 上位机程序：Node.js 语法通过；配置缺失时启动失败关闭；Windows 单文件构建及双向 TLS 的暂存、现场确认、提交、回读此前已完成联调。

## 浏览器结果

- 橘皮、色差、膜厚切换会清除旧预览并重新加载对应中文列定义；元数据就绪后模板与上传入口可用，无页面错误。
- 远程工作站代理对超过 64 KiB 的请求返回 413，不把大请求完整缓冲后再转发。
- 训练页面可同时选择“生产记录”和“人工训练数据”，两类来源没有优先级；人工入口提供中文 Excel/CSV 模板、名称和文件控件，不暴露 JSON 输入。
- 机器人远程工作站页面使用现场语言展示连接、隔离、审批和本机确认边界。

## CodeRabbit 状态

- 后端分段复审：14 项，13 项修复，1 项因字段本来不可修改判定为无效。
- 前端分段复审：8 项，全部处理；认证开关不按建议放宽，而是把入口代理收紧到前后端测试开关同时关闭。
- 上位机工具分段复审：3 项，全部修复。
- 数据库脚本分段复审首次受免费 CLI 14 分钟限流影响；等待后已重试总 DDL、三维布局增量、训练来源增量和独立 7 表 SQL 共 4 个文件，CodeRabbit 提出 0 项问题。

详细分类及证据见 `docs/coderabbit-remediation-plan.md`。
